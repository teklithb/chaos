# #!/bin/bash

# # Update your package list
#  sudo apt-get update && sudo apt-get install -y kubect
# # Install Required Dependencies
# sudo apt-get install -y apt-transport-https ca-certificates curl software-properties-common

# # Install VirtualBox Hypervisor
# sudo apt-get install -y virtualbox virtualbox-ext-pack

# # Install Minikube
# curl -Lo minikube https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64
# sudo chmod +x minikube
# sudo mv minikube /usr/local/bin/

# # Start Minikube
# minikube start

# # Check Minikube Status
# minikube status

# # Reminder to install kubectl if not already done
# echo "If you haven't already installed kubectl, please do so by following the official Kubernetes documentation."
